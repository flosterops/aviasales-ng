import { EmptyBucketDirective } from './empty-bucket.directive';

describe('EmptyBucketDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyBucketDirective();
    expect(directive).toBeTruthy();
  });
});
